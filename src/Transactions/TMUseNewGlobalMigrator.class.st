Class {
	#name : #TMUseNewGlobalMigrator,
	#superclass : #TMGlobalMigrator,
	#category : #Transactions
}

{ #category : #migrating }
TMUseNewGlobalMigrator >> migrateClassState: oldClass to: aClass [

	"Do nothing the new class have the correct state"
]
